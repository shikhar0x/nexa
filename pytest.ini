# Pytest configuration for the Nexa suite.
[pytest]
filterwarnings =
    # Third-party: chromadb's telemetry module calls a deprecated-on-3.14 API.
    # Nothing in Nexa code triggers it — remove once chromadb fixes upstream.
    ignore:'asyncio\.iscoroutinefunction' is deprecated:DeprecationWarning
